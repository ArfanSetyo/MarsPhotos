<?xml version="1.0" encoding="utf-8"?>
<!--
 Fungsi dari Android Manifest ini adalah untuk mengatur beberapa informasi yang akan ditampilkan di aplikasi yang sedang dibuat seperti informasi label, intent, icon
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.marsphotos">

    <!-- agar aplikasi kita dapat mengakses Internet, kita perlu menentukan izin ini. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarsPhotos">
        <activity android:name="com.example.android.marsphotos.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>